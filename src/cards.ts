// const cardsSection = document.querySelector('.about__cards') as HTMLElement;
// let activeCard = cardsSection?.querySelector('.card--active') as HTMLElement;

// const onCardClick = (evt: Event, callback: Function) => {
//     if (activeCard != evt.target) {
//         activeCard.classList.remove('card--active');
//         activeCard = evt.target as HTMLElement;
//         activeCard.classList.add('card--active')
//         document.querySelector('.about__information--active')?.classList.remove('about__information--active');
//         document.querySelector(`.about__information#${activeCard.id}`)?.classList.add('about__information--active');
//     }
// }
